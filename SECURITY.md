# セキュリティポリシー

## API キーの管理

### DeepL APIキー

このアプリケーションは翻訳機能にDeepL APIを使用します。APIキーは以下の方法で安全に管理されます：

1. **ローカル設定ファイル**
   - `data/settings.json`に保存（`.gitignore`に含まれる）
   - アプリ内の設定画面から入力可能

2. **環境変数（オプション）**
   - `.env.local`ファイルで管理
   - `DEEPL_API_KEY`環境変数

### ベストプラクティス

- ✅ APIキーをGitにコミットしない
- ✅ `.env*`ファイルは`.gitignore`に含める
- ✅ `data/`フォルダは`.gitignore`に含める
- ✅ 本番環境では環境変数を使用

## セキュリティ脆弱性の報告

セキュリティ上の問題を発見した場合は、公開のIssueではなく、以下の方法で報告してください：

- メール: [セキュリティ連絡先メールアドレス]
- GitHub Security Advisories

## サポートされるバージョン

| バージョン | サポート状況 |
| ------- | ---------- |
| 0.1.x   | ✅ サポート中 |

## 既知の制限事項

- DeepL APIキーはローカルストレージに保存されます
- ファイルシステムへのアクセス権限が必要です
